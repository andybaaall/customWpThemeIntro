// console.log('This console log comes from assets/js/script.js');

// $(document).ready throws an error, so we have to use the jQuery(document).ready syntax.
const $ = jQuery;

$(document).ready(() => {
	console.log('This jquery console log comes from script.js');
});
